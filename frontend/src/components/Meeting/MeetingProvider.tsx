import React from 'react';
import { MeetingProvider as VMeetingProvider } from '@videosdk.live/react-sdk';

const MeetingProvider = (props) => <VMeetingProvider {...props} />;

export default MeetingProvider;
